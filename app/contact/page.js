import ContactClient from '../../components/ContactClient';

export const metadata = {
  title: 'Contact Us | Manjula Binding Works Solapur',
};

export default function Contact() {
  return <ContactClient />;
}
